import { GeneroSeriePipe } from './genero-serie.pipe';

describe('GeneroSeriePipe', () => {
  it('create an instance', () => {
    const pipe = new GeneroSeriePipe();
    expect(pipe).toBeTruthy();
  });
});
