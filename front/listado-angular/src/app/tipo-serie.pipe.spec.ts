import { TipoSeriePipe } from './tipo-serie.pipe';

describe('TipoSeriePipe', () => {
  it('create an instance', () => {
    const pipe = new TipoSeriePipe();
    expect(pipe).toBeTruthy();
  });
});
